<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Trail Sync" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <!-- Application Header and Navigation -->
    <header class="bg-white shadow-lg border-b border-gray-100 sticky top-0 z-50">
      <nav class="container mx-auto flex justify-between items-center px-5 py-4">
        <%= link_to root_path, class: "flex items-center space-x-3 hover:opacity-80 transition duration-300" do %>
          <div class="bg-gradient-to-r from-blue-600 to-green-600 p-2 rounded-lg">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
          </div>
          <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-green-600 bg-clip-text text-transparent">TrailSync</h1>
        <% end %>
        
        <!-- Main Navigation -->
        <div class="hidden md:flex items-center space-x-6">
          <%= link_to "Home", root_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(root_path)}" %>
          <% if user_signed_in? %>
            <%= link_to "My Trips", trips_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(trips_path)}" %>
            <%= link_to "Things to Do", things_to_do_index_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(things_to_do_index_path)}" %>
            <%= link_to "Webcams", webcams_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(webcams_path)}" %>
            <%= link_to "New Trip", new_trip_path, class: "bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition duration-300 font-medium" %>
          <% else %>
            <%= link_to "All Trips", trips_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(trips_path)}" %>
            <%= link_to "Things to Do", things_to_do_index_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(things_to_do_index_path)}" %>
            <%= link_to "Webcams", webcams_path, class: "text-gray-700 hover:text-blue-600 font-medium transition duration-300 #{'text-blue-600 font-semibold' if current_page?(webcams_path)}" %>
          <% end %>
        </div>

        <!-- User Menu -->
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <div class="hidden md:flex items-center space-x-3">
              <span class="text-gray-600">Welcome back!</span>
              <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-600 hover:text-red-500 transition duration-300 font-medium" %>
            </div>
          <% else %>
            <div class="flex items-center space-x-3">
              <%= link_to 'Sign In', new_user_session_path, class: "text-blue-600 hover:text-blue-800 transition duration-300 font-medium" %>
              <%= link_to 'Sign Up', new_user_registration_path, class: "bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition duration-300 font-medium" %>
            </div>
          <% end %>
          
          <!-- Mobile Menu Button -->
          <button class="md:hidden p-2 rounded-lg hover:bg-gray-100" onclick="toggleMobileMenu()">
            <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
      </nav>
      
      <!-- Mobile Navigation -->
      <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-100">
        <div class="container mx-auto px-5 py-4 space-y-3">
          <%= link_to "Home", root_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
          <% if user_signed_in? %>
            <%= link_to "My Trips", trips_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
            <%= link_to "Things to Do", things_to_do_index_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
            <%= link_to "Webcams", webcams_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
            <%= link_to "New Trip", new_trip_path, class: "block bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition duration-300 font-medium text-center" %>
          <% else %>
            <%= link_to "All Trips", trips_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
            <%= link_to "Things to Do", things_to_do_index_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
            <%= link_to "Webcams", webcams_path, class: "block text-gray-700 hover:text-blue-600 font-medium transition duration-300" %>
          <% end %>
        </div>
      </div>
    </header>
    
    <!-- Background -->
    <div class="min-h-screen bg-gray-50">
      <main class="container mx-auto px-5 py-8">
        <!-- Notice and Alert messages -->
        <% if notice.present? %>
          <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg mb-6 flex items-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <%= notice %>
          </div>
        <% end %>
        
        <% if alert.present? %>
          <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg mb-6 flex items-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <%= alert %>
          </div>
        <% end %>
        
        <%= yield %>
      </main>
    </div>
    
    <script>
      function toggleMobileMenu() {
        const menu = document.getElementById('mobile-menu');
        menu.classList.toggle('hidden');
      }
      
      // Close mobile menu when clicking outside
      document.addEventListener('click', function(event) {
        const menu = document.getElementById('mobile-menu');
        const button = event.target.closest('button');
        
        if (!menu.contains(event.target) && !button) {
          menu.classList.add('hidden');
        }
      });
    </script>
  </body>
</html>
